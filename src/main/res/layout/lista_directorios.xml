<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">
    <RelativeLayout
        android:layout_centerInParent="true"
        android:id="@+id/contenedorSinContactos"
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent">
        <ImageView
            android:id="@+id/imgBackgroundContactos"
            android:layout_centerInParent="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/img_background_contacts"/>
        <TextView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/imgBackgroundContactos"
            android:textColor="@color/primary_text"
            android:gravity="center"
            android:text="Lo sentimos, no se obtuvo \n ningún directorio con archivos mp3."/>
    </RelativeLayout>

    <ListView
        style="@style/StyleListView"
        android:id="@+id/list"/>

</RelativeLayout>