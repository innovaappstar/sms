<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:id="@+id/relativeLayoutItemBluetooth"
    android:background="@drawable/shape_row_selector">
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/ic_contacto"
        android:background="@drawable/ic_bluetooth_small"/>
    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingLeft="10dp"
        android:paddingStart="10dp"
        android:paddingRight="10dp"
        android:paddingEnd="10dp"
        android:layout_toRightOf="@+id/ic_contacto"
        android:layout_toEndOf="@+id/ic_contacto">

        <TextView
            style="@style/StyleTextoItem"
            android:textColor="@color/gris_fuerte"
            android:textSize="12sp"
            android:text="MacAddress"/>
        <TextView
            style="@style/StyleTextoItem"
            android:textColor="@color/gris_fuerte"
            android:id="@+id/txtTextoMacAddress"
            android:textSize="12sp"
            android:text=""/>
    </LinearLayout>


</RelativeLayout>